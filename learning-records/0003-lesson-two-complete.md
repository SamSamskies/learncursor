# Lesson 2 complete: four-part prompts in practice

The user completed Lesson 2 by writing and executing a four-part Agent prompt outside this workspace — step 1 of a Plan for an Express + SQLite app with a `/hello` endpoint and an `npm test` done-when check. They can structure Goal, Scope, Constraints, and Done-when without re-teaching the template. Future sessions should assume prompt anatomy fluency and move to persistent context (rules) and the verify-and-iterate loop.

**Evidence:** User pasted their full four-part prompt (Express, sqlite3, seed "world", test `/hello` returns `hello ${wordFromDb}`, run `npm test`).

**Implications:** Lesson 3 (project rules) is the correct next step — encode the stack and test conventions they repeated in Constraints so later plan steps need shorter prompts. Habit-tracker exercise from Lesson 1 was done in Plan Mode only; the build practice happened in a separate Express project, which is fine.
