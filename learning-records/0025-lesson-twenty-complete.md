# Lesson 20 complete — Terminal Integration

The user completed Lesson 20: practiced `@Terminals` for failure debugging, ran verify themselves after Agent fixed a test, and confirmed the Cursor CLI (`agent about`, headless read with `-p --trust`). They understand the three terminal surfaces — integrated (Agent default), external (hook tests, bypass live hooks), CLI (quick sessions and headless scripts).

**Implications:** Terminal context and CLI are live skills. Next per curriculum: Git Workflows — `@Commit`, `@Branch`, SCM vs Agent commits, `gh` PR drafting. Background Agents follows after that.
