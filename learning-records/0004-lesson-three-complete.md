# Lesson 3 complete: project rules carry Constraints

The user created an `express-api` project rule and sent a thin prompt for POST /habits — Goal, Scope, and Done-when only, no Constraints section. Agent followed the stack and test conventions from the rule without being told again. They can set up Always Apply rules and trust them to replace repeated prompt boilerplate.

**Evidence:** User reported completing lesson 3; no Constraints in the POST /habits prompt; agent followed express-api rule conventions.

**Implications:** Rules fluency is established. Next: the verify-and-iterate loop — reading diffs, running done-when checks, and choosing the right fix surface when something fails. Continue the Express habit-tracker plan with thin prompts + rules as the default workflow.
