# Lesson 19 complete — Run Modes (YOLO Mode)

The user completed Lesson 19: configured Auto-review (or equivalent), added `.cursor/permissions.json` with block instructions for destructive git, and ran the verify loop with minimal approval clicks while still reading diffs. They understand the stack — Run Modes reduce prompts, hooks block deterministically, verify loop confirms done-when.

**Implications:** Terminal friction is reduced for the solo-builder loop. Next per curriculum: Terminal Integration — `@Terminals`, integrated vs external terminal, Cursor CLI. Git Workflows follows after that.
