# Lesson 9 complete: PR review loop on bitcoin-connect

The user triaged CodeRabbit feedback on [PR #395](https://github.com/getAlby/bitcoin-connect/pull/395) using the four-step review loop: separate prompts per comment, minimal scoped commits, local keyboard verify, and push. Two fix commits landed — `aria-label` on the dialog panel, and a `Map` to preserve each sibling's prior `inert` state on teardown instead of blanket reset.

**Evidence:** User reported lesson 9 done after fixing both CodeRabbit threads. PR now has 5 commits total; both inline comments marked resolved; CI pre-merge checks passing.

**Implications:** The contributor side of the OSS arc is complete through review response. Next teaching is the passive half — re-request review, maintainer etiquette, knowing when your job is done, and staying productive while waiting. Mission success criteria are all demonstrated; future sessions can deepen Cursor power-user skills or start fresh app ideas without re-teaching basics.
