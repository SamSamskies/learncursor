# Lesson 18 complete: Cursor iOS — cloud agents + Remote Control

The user finished Lesson 18 and verified both mobile workflows end-to-end: starting a cloud agent from iOS (notification while phone locked, same run visible on desktop) and handing off a local Agents Window session via `/remote-control` to continue from the phone. They also discovered that Remote Control settings and the slash command only appear in the **Agents Window** settings panel — not in the regular Editor Window settings — which is a common UX trap.

**Evidence:** User reported lesson 18 complete; confirmed cloud agent and Remote Control both work with the iOS app after enabling Remote Control in Agents Window → Settings → Agents.

**Implications:** Mobile is now a live control surface for the solo-builder mission (kick off, walk away, review, merge). iOS inbox shows cloud agents automatically; local desktop sessions require `/remote-control` or Move to Cloud. Next per NOTES: post–Lesson 18 curriculum — YOLO Mode, Terminal Integration, Git Workflows, Background Agents, Custom Modes, Memories. LocalLab backlog (DB MCP, OCR, config stack port) still available when user wants app-building topics.
