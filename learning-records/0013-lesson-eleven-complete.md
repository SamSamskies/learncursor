# Lesson 11 complete: shell audit hook on habit tracker

The user installed and tested a project hook on their habit tracker: an observe-only shell audit log via `afterShellExecution`, following the five-step setup loop (script → Terminal.app test → hooks.json → Agent trigger → commit).

**Evidence:** User reported lesson 11 done after creating and testing the shell audit hook in the habit tracker app.

**Implications:** Hooks layer is live on a real project. Next: Agent Skills — reusable multi-step workflows loaded on demand (verify loop, ship checklist) as the complement to rules (always-on prose) and hooks (deterministic gates). Automations and cloud agents remain after skills.
